<%= form_for(@integrant) do |f| %>
  <% if @integrant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@integrant.errors.count, "error") %> prohibited this integrant from being saved:</h2>

      <ul>
      <% @integrant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :orchestra_id %><br>
    <%= f.number_field :orchestra_id %>
  </div>
  <div class="field">
    <%= f.label :instrument_id %><br>
    <%= f.number_field :instrument_id %>
  </div>
  <div class="field">
    <%= f.label :conductor_id %><br>
    <%= f.number_field :conductor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
